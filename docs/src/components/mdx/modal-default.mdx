```jsx
<Button text="Open Modal" onClick={() => setModal(true)} />
<Modal onClick={() => setModal(false)} open={modal}>
  <h1>Title</h1>
  <p>This is some placeholder content</p>
</Modal>
<br />
<Button text="Open Modal" onClick={() => setModal(true)}/>
<Modal onClick={() => setModal(false)} open={modal} bg="black" color="white">
  <h1>Title</h1>
  <p>This is some placeholder content</p>
</Modal>
```